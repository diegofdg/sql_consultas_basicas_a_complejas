#* Calcula cual es el máximo valor de la columna length de la tabla FILM *#
SELECT MAX(length) FROM sakila.film;

#* Calcula cual es el mínimo valor de la columna length de la tabla FILM *#
SELECT MIN(length) FROM sakila.film;

#* Calcula cual es el mínimo valor de la columna replacement_cost de la tabla FILM *#
SELECT MIN(replacement_cost) FROM sakila.film;

#* Calcula cual es el máximo valor de la columna replacement_cost de la tabla FILM *#
SELECT MAX(replacement_cost) FROM sakila.film;